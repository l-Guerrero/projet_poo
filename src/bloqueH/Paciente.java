package bloqueH;

public class Paciente {
    String nombre;
    int edad;
    String sintoma;

    /* Cuando decimos "crear un paciente" estamos reconociendo que necesitamos registrar a
    una persona nueva que llegó al hospital, es solo una idea abstracta en la que solo se piensa
    que hay que crearla sin llegar a pensar más allá, es decir, no se piensa cómo se
    va a hacer solo que se va a hacer.
    en cambio, "instanciar la clase paciente" es darle vida a esa idea en el computador. Instanciar
    significa que lo que escribimos en el codigo finalmente tiene un lugar en la memoria.
    resumido "crear un paciente" solo describe que queremos hacer, mientras que "instanciar"
    describe cómo lo logra el software*/
}
